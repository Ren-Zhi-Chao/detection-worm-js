import ObjectType from './src/core/object_type';
import WebLoader from './src/core/web_loader';
import XPath from './src/core/XPath';

export default {
    version: '1.0.0',
    help: () => console.log(`包含方法（详情请看doc）：
    ObjectType, WebLoader, XPath
    `)
}

export {
    ObjectType, WebLoader, XPath
}

